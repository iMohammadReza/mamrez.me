---
import SocialChip from './SocialChip.astro';
import { socials } from '../lib/config';
---

<h2 class="section-title">Contact me</h2>
<div class="mt-4 mb-8">
  {
    socials.map(({ title, link, iconName }) => (
      <SocialChip title={title} link={link} iconName={iconName} />
    ))
  }
</div>
